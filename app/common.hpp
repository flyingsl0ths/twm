#pragma once

#include <cstddef>
#include <string_view>

using u8    = char8_t;
using u32   = unsigned int;
using usz   = std::size_t;
using s32   = int;
using f32   = float;
using f64   = double;
using usize = std::size_t;
using str   = std::string_view;
